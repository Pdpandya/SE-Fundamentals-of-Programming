#include <stdio.h>

int main() {
    int num = 972;

    // Printing numbers from 972 to 897 using a while loop
    while (num >= 897) {
        printf("%d ", num);
        num--;
    }

    printf("\n");

    return 0;
}

